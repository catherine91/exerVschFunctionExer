function addTwoNumber(x, y) { 
  return x + y;
}

function returnLargest(x, y, z) {
  return Math.max(x, y, z);
}

function evenOrOdd(x) {
  if (x % 2 === 0) {
    return "even";
  } else {
    return "odd";
  }
}